/** Placeholder do layout autenticado — a listagem real de chamados é o próximo marco. */
export function DashboardPlaceholder() {
  return (
    <div>
      <h2>Bem-vindo(a) de volta.</h2>
      <p>A listagem de chamados chega no próximo marco.</p>
    </div>
  );
}
